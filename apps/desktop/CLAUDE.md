# CLAUDE.md

Canonical desktop-frontend agent guidance now lives in `AGENTS.md`.

Claude Code should read this directory's `AGENTS.md` first, then the root `../../AGENTS.md` for repo-wide guidance. The local guide covers the app shell and routing, the TanStack Query layer, the typed Tauri event fabric, issue lifecycle and gating, fix guides, and the mandatory styling and accessibility rules. `src/styles/COMPONENT_GUIDE.md` is the authoritative styling contract. The Rust backend has its own guide at `src-tauri/AGENTS.md`.
