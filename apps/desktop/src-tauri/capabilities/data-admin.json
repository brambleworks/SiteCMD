{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sitecmd-data-admin",
  "description": "Elevated SiteCMD data administration commands. The bridge window only receives the broker dispatch permission; the underlying delete/import commands are reachable as regular Rust function calls inside run_data_admin_command, never as direct Tauri IPC calls. This forces every elevated invocation through token_state.consume() and any native confirmation the command itself requires.",
  "windows": ["data-admin"],
  "permissions": ["core:event:default", "allow-run-data-admin-command"]
}
