{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sitecmd-external-connectors",
  "description": "Elevated SiteCMD connector commands. The bridge window only receives the broker dispatch permission; OAuth callbacks, license activation, integration writes, etc. are reachable as regular Rust function calls inside run_external_connector_command, never as direct Tauri IPC calls. This forces every elevated invocation through token_state.consume() so a one-time token cannot be skipped.",
  "windows": ["external-connectors"],
  "permissions": ["core:event:default", "allow-run-external-connector-command"]
}
